# Introduction

```remark
This file is named like its folder, so it carries the chapter title. The sections of
this chapter are separate files in the "Subchapters" folder next to it.

The rule this template follows: a .md file NEVER starts with more than one #.
One # always means "the level of the folder I am in" - so every file reads the same
way no matter how deep it sits, and you never count # signs. The level comes from
where the file lies, which is why moving a file never means editing its headings.

Why the collector folder: it creates the section level once, for all sections
together, instead of one folder per section.

When a section grows its own subsections, give it its own folder: create a folder
next to "Subchapters", put a file with the SAME name inside, move the section text
there, and add a "Subchapters" folder for its parts. That keeps the section and its
parts together when you move them.
```

Replace this paragraph with your introduction.
